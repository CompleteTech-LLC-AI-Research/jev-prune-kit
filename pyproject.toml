[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "jev-prune-kit"
version = "0.1.0"
description = "Capability-aware context-pruning installer and experimental harness adapters"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
jev-prune = "jev_prune.cli:main"

[tool.setuptools]
packages = ["jev_prune"]
